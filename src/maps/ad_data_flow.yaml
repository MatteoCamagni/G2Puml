g2p:
  Given:
    - 'the (?P<runner>.*) during the SSM\.(?P<ssm>\w+) state'
  When:
    - 'the (?P<swc>.*) reads the (?P<sgn>\w+) signal'
  Then:
    - 'it shall (evaluate the (?P<sgn>\w+) signal)|(execute the (?P<cmd>\w+) command)'
